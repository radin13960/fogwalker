# تصویر پروژه برای تغییرات آینده (Snapshot)

## این فایل چیست؟

**`FogWalkerTPS_Full.zip`** فشرده‌ی کاملِ همین پروژه (همه‌ی کدها، ورودی‌اکشن، بسته‌ها، اسناد و ابزارها) است. این فایل «حافظه‌ی» پروژه شما محسوب می‌شود.

## برای چه استفاده می‌شود؟

هر وقت خواستید **تغییری در بازی بدهید** (افزودن سلاح/دشمن/مرحله، تغییر متن‌ها، تغییر تنظیمات، رفع باگ، بهینه‌سازی…):

1. 📎 فایل `FogWalkerTPS_Full.zip` را در گفتگو با دستیار آپلود کنید.
2. ✍️ درخواست خود را بنویسید — فارسی یا انگلیسی. مثال‌ها:
   - «یک سلاح مینی‌گن با مگزین ۱۰۰ اضافه کن»
   - «مرحله‌ی چهارم با تم شب بساز»
   - «دمیج شاتگان را کم کن»
   - «متن منوی اصلی را عوض کن»
3. ✅ تغییرات روی پروژه اعمال می‌شود و یک ZIP به‌روز تحویل می‌گیرید تا دفعه‌ی بعد همان را آپلود کنید.

## نکته‌های مهم

- **همیشه آخرین ZIP را نگه دارید.** هر دور تغییر، نسخه‌ی تازه‌ای می‌سازد؛ نسخه‌های قدیمی را آرشیو کنید یا حذف کنید.
- این ZIP شامل پوشه‌ی `Library/` و `Build/` نیست — آن‌ها روی سیستم شما بعد از باز کردن پروژه در Unity ساخته می‌شوند.
- بعد از هر تغییر، کافیست در Unity دوباره `FogWalker > Setup > 0` و سپس `FogWalker > Build > APK` را بزنید تا نسخه‌ی نصبی تازه بگیرید.
- اگر جایی (مثلاً Git) از پروژه نسخه‌برداری می‌کنید، همین ZIP معادل یک commit کامل است.

## محتویات ZIP

```
FogWalkerTPS/
├─ Assets/_Project/…   (کد Runtime + Editor + ورودی + تست‌ها)
├─ Packages/manifest.json
├─ Docs/               (۷ سند راهنما)
├─ tools/              (اسکریپت ساخت APK/AAB)
├─ README.md
├─ SNAPSHOT.md         (همین فایل)
└─ .gitignore
```

*نسخه‌ی تصویر: ۱٫۰ — ۲۰۲۶*
